{"scripts": {
  "start": "ts-node-dev --respawn --transpile-only src",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "dependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  }
  
}
